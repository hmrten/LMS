<div class="header header-logo">LMS</div>

@if (Request.IsAuthenticated)
{
    <div class="login-holder">
        <div class="login-name">Inloggad som: @User.Identity.Name</div>
        <div class="login-link">@Html.ActionLink("Logga ut", "Logout", "User")</div>
        <div class="clear">&nbsp;</div>
    </div>
}
else
{
    <div class="login-holder">
        <div class="login-link">@Html.ActionLink("Logga in", "Login", "Home")</div>
    </div>
}

<div class="clear">&nbsp;</div>